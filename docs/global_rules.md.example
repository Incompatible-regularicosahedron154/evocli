# EvoCLI 全局规则 — 适用于所有项目
# ======================================
# 文件位置：~/.evocli/global_rules.md
# 这些规则在没有项目级 AGENTS.md 时生效
# 项目级规则（AGENTS.md）优先级更高
#
# 创建方式：将此文件复制到 ~/.evocli/global_rules.md

## 通用工程规范

### 代码质量
- 优先编写自文档化代码，减少注释噪音
- 函数长度超过 30 行时考虑拆分
- 循环复杂度超过 10 时考虑重构
- 避免深层嵌套（超过 3 层时使用 early return）

### 安全习惯
- 不在代码中硬编码任何密钥、密码或 token
- 用户输入必须验证和清理
- SQL 查询使用参数化而非字符串拼接

### 版本控制习惯
- 提交信息使用 Conventional Commits 格式
  - feat: 新功能
  - fix: bug 修复
  - refactor: 重构（不改变外部行为）
  - docs: 文档
  - test: 测试
  - chore: 维护性工作
- 每次提交只做一件事

### 测试习惯
- 修改公共函数前，先确认是否有测试
- Bug 修复必须附带回归测试
- 测试名称描述行为，而非实现

## AI 行为偏好

### 沟通风格
- 用中文回复（除非用户用英文提问）
- 代码修改前先说明原因（1-2 句话）
- 不要解释显而易见的代码
- 对不确定的判断，表达出不确定性

### 工作习惯
- 优先使用最小化修改
- 不主动重构不相关的代码
- 遇到 FIXME 时询问是否需要修复，而非自动修复
